# Agents

## 基本信息

```yaml
name: data-analyst
display_name: 数据分析助手
description: >
  专注于数据查询与分析的 OpenClaw Subagent。连接企业语义层指标平台，
  支持指标查询、归因分析、可视化仪表板生成和飞书卡片输出。
version: v1.0.0
```

## 触发关键词

当用户输入包含以下关键词时，OpenClaw 应将请求路由到本 subagent：

**数据查询类**
- 查询指标、查指标、指标数据、指标查询
- 查数据、查一下、数据查询
- GMV、转化率、客单价、订单量（及其他业务指标名称）

**分析类**
- 数据分析、分析一下、归因分析、指标归因
- 异动分析、下降原因、增长原因、原因分析
- 同比、环比、对比、趋势
- 异常检测、异常、巡检、健康检查、有没有异常
- 预测、预估、月底能到多少、完成目标、趋势推演

**报告类**
- 出一份报告、月报、周报、数据报告、数据复盘、经营分析

**定时类**
- 定时报告、定期执行、每周、每月、以后都

**输出类**
- 仪表板、dashboard、可视化、图表
- 飞书卡片、卡片、播报、数据报告
- 生成报表、做个图

**探查类**
- 有哪些指标、指标列表、维度、数据维度

## 技能列表

### metric-query
- **描述**: 构建语义层指标查询请求，支持时间筛选、维度分组、同环比/占比/排名等快速衍生计算
- **触发条件**: 用户需要查询具体指标数值、做多维分析、计算同环比时

### metric-attribution
- **描述**: 对指标异动进行多维度归因拆解，识别贡献因子，生成结构化归因报告
- **触发条件**: 用户询问指标为何上涨/下降、需要归因分析、排查异动原因时

### dip-vap-dashboard
- **描述**: 将查询数据生成可交互的单文件 HTML 仪表板，支持 Chart.js 图表、下拉筛选器，无需服务器直接浏览器打开
- **触发条件**: 数据量 >20 行、包含时序/多维数据、或用户明确要求可视化时

### dip-vap-feishu-card
- **描述**: 将数据分析结果封装为飞书消息卡片，支持 KPI 摘要、趋势折线、排行榜、对比表等模板
- **触发条件**: 用户需要生成飞书卡片、数据播报、或在飞书中分享分析结果时

### anomaly-detection
- **描述**: 对指标进行异常检测，判断当前数据是否偏离正常范围，输出结构化异常检测报告；所有数据查询委派 metric-query 执行
- **触发条件**: 用户询问指标是否正常/异常、做健康巡检/异常扫描、检查数据有没有问题时

### forecast-simulation
- **描述**: 基于历史数据进行趋势预测、目标缺口分析、What-if 场景模拟（数学推算，非 ML 模型），结果附带前提假设和不确定性说明
- **触发条件**: 用户询问未来走势、目标是否可达、模拟假设场景（"月底能到多少""提升10%会怎样"）时

### analysis-report
- **描述**: 编排并生成完整的多板块数据分析报告，调度 metric-query、anomaly-detection、metric-attribution、forecast-simulation 执行各板块，将结果串联为连贯分析叙事
- **触发条件**: 用户需要一份完整的、多板块分析文档时（"出月报""做分析报告""数据复盘"等）；若含"以后/每月/定期"等重复意图则转 scheduled-report

### scheduled-report
- **描述**: 将当前对话中已完成的分析流程提取、打包为自包含 prompt，通过 `openclaw cron add` 固化为定时执行任务（周报/月报/日巡检等）
- **触发条件**: 用户表达中含"定时/定期/每X/以后都"等重复执行意图时；前提：对话中需有已完成的分析历史，否则先引导完成分析

## 必需配置

| 配置项 | 说明 | 配置位置 |
|--------|------|----------|
| `CAN_API_KEY` | 语义层 Gateway API 的认证密钥，格式为 `cgk-xxxxxxxx` | `~/.openclaw/.env` |

配置示例：
```bash
# ~/.openclaw/.env
CAN_API_KEY=cgk-your-api-key-here
```

> 未配置或配置无效的 CAN_API_KEY 将导致指标查询功能不可用（返回 401）。

## 注册信息

```yaml
agent_id: data-analyst
repo: openclaw-subagent-data-analyst
entry: AGENTS.md
```
