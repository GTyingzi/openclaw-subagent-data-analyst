# Agents

## 基本信息

```yaml
name: data-analyst
display_name: 数据分析助手
description: >
  专注于数据查询与分析的 OpenClaw Subagent。连接企业语义层指标平台，
  支持指标查询、归因分析、可视化仪表板生成和飞书卡片输出。
version: v1.0.0
```

## 触发关键词

当用户输入包含以下关键词时，OpenClaw 应将请求路由到本 subagent：

**数据查询类**
- 查询指标、查指标、指标数据、指标查询
- 查数据、查一下、数据查询
- GMV、转化率、客单价、订单量（及其他业务指标名称）

**分析类**
- 数据分析、分析一下、归因分析、指标归因
- 异动分析、下降原因、增长原因、原因分析
- 同比、环比、对比、趋势

**输出类**
- 仪表板、dashboard、可视化、图表
- 飞书卡片、卡片、播报、数据报告
- 生成报表、做个图

**探查类**
- 有哪些指标、指标列表、维度、数据维度

## 技能列表

### metric-query
- **描述**: 构建语义层指标查询请求，支持时间筛选、维度分组、同环比/占比/排名等快速衍生计算
- **触发条件**: 用户需要查询具体指标数值、做多维分析、计算同环比时

### metric-attribution
- **描述**: 对指标异动进行多维度归因拆解，识别贡献因子，生成结构化归因报告
- **触发条件**: 用户询问指标为何上涨/下降、需要归因分析、排查异动原因时

### dip-vap-dashboard
- **描述**: 将查询数据生成可交互的单文件 HTML 仪表板，支持 Chart.js 图表、下拉筛选器，无需服务器直接浏览器打开
- **触发条件**: 数据量 >20 行、包含时序/多维数据、或用户明确要求可视化时

### dip-vap-feishu-card
- **描述**: 将数据分析结果封装为飞书消息卡片，支持 KPI 摘要、趋势折线、排行榜、对比表等模板
- **触发条件**: 用户需要生成飞书卡片、数据播报、或在飞书中分享分析结果时

## 必需配置

| 配置项 | 说明 | 配置位置 |
|--------|------|----------|
| `CAN_API_KEY` | 语义层 Gateway API 的认证密钥，格式为 `cgk-xxxxxxxx` | `~/.openclaw/.env` |

配置示例：
```bash
# ~/.openclaw/.env
CAN_API_KEY=cgk-your-api-key-here
```

> 未配置或配置无效的 CAN_API_KEY 将导致指标查询功能不可用（返回 401）。

## 注册信息

```yaml
agent_id: data-analyst
repo: openclaw-subagent-data-analyst
entry: AGENTS.md
```
