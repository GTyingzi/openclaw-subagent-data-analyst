# Bootstrap

本文件定义 **数据分析助手（data-analyst）** 的启动初始化规范。

---

## 启动前环境检查

在 subagent 首次响应用户请求之前，应完成以下环境检查：

### 检查清单

| # | 检查项 | 检查方式 | 失败类型 |
|---|--------|----------|----------|
| 1 | `CAN_API_KEY` 已配置 | 检查 `~/.openclaw/.env` 中是否存在 `CAN_API_KEY=cgk-...` | 硬失败 |
| 2 | `CAN_API_KEY` 格式有效 | 验证值以 `cgk-` 前缀开头且非空 | 硬失败 |
| 3 | 技能文件完整 | 确认 `skills/` 目录下 4 个技能目录均存在 | 软失败 |
| 4 | MCP 工具可达（可选） | 尝试调用 `getMetricSpaceListUsingGET` 验证连通性 | 软失败 |

---

## 初始化加载顺序

### 日常启动（非首次）

1. 加载 `~/.openclaw/.env` 环境变量（获取 `CAN_API_KEY`）
2. 加载项目 `config.json`（读取本地配置）
3. 验证 `CAN_API_KEY` 格式
4. 加载记忆文件（`MEMORY.md` 及 memory/ 目录下的分主题文件）
5. 完成初始化，等待用户指令

### 首次启动 ⭐

> 以下步骤**仅首次启动时执行**，日常启动跳过。

1. 检查 `~/.openclaw/` 目录是否存在，不存在则创建
2. 检查 `~/.openclaw/.env` 是否存在，不存在则提示用户配置（见下方提示模板）
3. 检查记忆目录是否存在，不存在则创建
4. 初始化空白的 `MEMORY.md` 主记忆文件（如不存在）
5. 执行完整的环境检查清单（见上方）
6. 向用户确认初始化成功

---

## 失败处理策略

### 硬失败（阻止启动）

当 `CAN_API_KEY` 未配置或格式无效时，**阻止执行指标查询功能**，并向用户展示以下提示：

```
⚠️ 数据分析助手初始化失败

原因：未找到有效的 CAN_API_KEY 配置。

请按以下步骤修复：
1. 打开或创建文件 ~/.openclaw/.env
2. 添加一行：CAN_API_KEY=cgk-your-api-key-here
3. 保存文件后重新开始对话

如需获取 API Key，请联系数据平台管理员。
```

### 软失败（降级运行）

当技能文件不完整或 MCP 服务不可达时，**不阻止启动**，但需告知用户受影响的功能：

```
⚠️ 部分功能不可用

以下技能加载失败，相关功能暂时不可用：
- dip-vap-dashboard：仪表板生成不可用

其他功能正常运行。如需修复，请重新安装本 subagent。
```

---

## 首次启动用户提示模板

```
👋 欢迎使用数据分析助手（data-analyst）！

在开始之前，需要配置 API Key：

1. 打开终端，执行：
   mkdir -p ~/.openclaw
   echo "CAN_API_KEY=cgk-your-api-key-here" >> ~/.openclaw/.env

2. 将 cgk-your-api-key-here 替换为你的真实 API Key

3. 重新开始对话即可使用全部功能

如需获取 API Key，请联系数据平台管理员。
```
