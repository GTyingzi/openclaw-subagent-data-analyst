# Memory

本文件定义 **数据分析助手（data-analyst）** 的持久化记忆规范，指导跨会话的知识积累与管理。

---

## 存储结构

记忆文件存储于 Claude Code 项目记忆目录：

```
~/.claude/projects/-Users-{username}-{project-path}/memory/
```

对于本项目，路径通常为：
```
~/.claude/projects/-Users-guotao-PycharmProjects-openclaw-subagent-data-analyst/memory/
```

### 文件组织

| 文件 | 用途 |
|------|------|
| `MEMORY.md` | 主记忆文件，始终加载入上下文（行数超过 200 行后内容被截断，保持简洁） |
| `metrics.md` | 常用指标名称、编码、口径备忘 |
| `dimensions.md` | 常用维度及维度值映射 |
| `user-preferences.md` | 用户个性化偏好记录 |
| `patterns.md` | 稳定的查询模式和问题解法 |

---

## 应记录的内容

### 1. 用户偏好
用户明确表达或多次体现的个性化偏好。

示例：
- "用户偏好用万为单位显示数字，而非原始值"
- "用户习惯看近 30 天对比上月同期，这是默认时间范围"
- "用户的飞书卡片首选 kpi_summary 模板"

### 2. 常用指标与口径
多次出现的指标名称、编码和口径说明，避免每次重新搜索。

示例：
- `GMV` → 指标编码 `gross_merchandise_value`，含义：成交总额（含退款）
- `CVR` → 指标编码 `conversion_rate`，口径：下单用户数 / 访客数
- `DAU` → 指标编码 `daily_active_users`

### 3. 维度映射
常用维度的编码和常见维度值。

示例：
- 渠道维度 → `dim_channel`，常见值：`直播`、`搜索`、`推荐`
- 品类维度 → `dim_category_l1`，顶级品类列表

### 4. 查询模式
稳定的查询结构和问题解法，供同类问题复用。

示例：
- "同环比查询固定使用 `metric_time__month` 维度 + `sameperiod__yoy__growthvalue` 衍生"
- "多渠道对比时默认按 GMV 降序排列"

---

## 不应记录的内容

- **临时查询结果**：单次会话的数据查询输出（数字会过期失效）
- **敏感信息**：API Key、用户密码、个人身份信息
- **会话临时状态**：当前正在讨论的具体问题、中间计算步骤
- **未经验证的结论**：仅凭单次观察得出的假设性结论
- **重复或过时内容**：更新记忆时先检查是否已有相似条目，有则更新而非新增

---

## 记忆更新触发条件

以下情况发生时应更新记忆文件：

1. **用户明确要求记住某事** — "记住我习惯用万做单位"、"把这个指标的口径记下来"
2. **发现稳定模式** — 同一类问题在多次对话中反复出现，且解法固定
3. **纠正错误认知** — 用户指出之前的记忆条目有误，必须立即更新或删除错误条目
4. **首次确认重要配置** — 如用户确认了某个指标的正确编码或口径

---

## 记忆维护原则

- `MEMORY.md` 主文件保持 **200 行以内**，详细内容放在独立的分主题文件中
- 记忆按**主题语义**组织，而非按时间顺序堆叠
- 发现错误或过时条目时**立即更新**，不保留矛盾内容
- 写入记忆前**先检查是否已有相似条目**，避免重复
